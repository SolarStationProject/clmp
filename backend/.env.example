# Servidor
PORT=3000
NODE_ENV=development

# Base de Datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cleanmap
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# JWT
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# CORS (separar con comas si hay múltiples orígenes)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Upload
UPLOAD_MAX_SIZE_MB=5

# Nodemailer — Gmail SMTP con App Password (sin espacios en MAIL_PASS)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu_cuenta@gmail.com
MAIL_PASS=xxxx xxxx xxxx xxxx
# URL del frontend para los enlaces en emails (cambiar al dominio de Netlify en producción)
FRONTEND_URL=http://localhost:5173
